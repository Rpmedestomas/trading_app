Approved Users:
<% @users.each do |user|%>
    <% if user.user_status == 'Approved' %>
        <p>
        Email: <%= user.email %>
        Full name: <%= user.full_name %> 
        <%= link_to 'Show', admin_path(user) ,class:"text-blue-500" %>
        <%= link_to 'Edit', edit_admin_path(user) ,class:"text-green-500" %>
        </p>
    <% end %>
<%end%>

Pending Users:
<% @users.each do |user|%>
    <% if user.user_status == 'Pending' %>
        <p>
        Email: <%= user.email %>
        Full name: <%= user.full_name %> 
        <%= link_to 'Approve', approve_user_path(user) ,class:"text-green-500" %>
        <%= link_to 'Reject', reject_user_path(user) ,class:"text-green-500" %>
        </p>
    <% end %>
<%end%>